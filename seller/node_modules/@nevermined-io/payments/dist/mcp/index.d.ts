/**
 * MCP (Model Context Protocol) integration module for Nevermined Payments.
 *
 * This module provides everything needed to build monetizable MCP servers:
 * - Paywall protection for tools, resources, and prompts
 * - OAuth 2.1 discovery and client registration endpoints
 * - Managed HTTP server or router for existing Express apps
 *
 * @example Basic usage with withPaywall
 * ```typescript
 * import { Payments } from '@nevermined-io/payments'
 *
 * const payments = Payments.getInstance({
 *   nvmApiKey: process.env.NVM_API_KEY!,
 *   environment: 'staging_sandbox'
 * })
 *
 * // Configure MCP integration
 * payments.mcp.configure({
 *   agentId: process.env.NVM_AGENT_ID!,
 *   serverName: 'my-mcp-server'
 * })
 *
 * // Protect a tool handler
 * const protectedHandler = payments.mcp.withPaywall(
 *   myToolHandler,
 *   { kind: 'tool', name: 'my_tool', credits: 1n }
 * )
 * ```
 *
 * @example Complete server with OAuth
 * ```typescript
 * // Start a managed server with all OAuth endpoints
 * const { baseUrl, stop } = await payments.mcp.startServer({
 *   port: 5001,
 *   agentId: process.env.NVM_AGENT_ID!,
 *   serverName: 'my-mcp-server',
 *   tools: ['hello_world']
 * })
 * ```
 *
 * @example Using router with existing Express app
 * ```typescript
 * import express from 'express'
 *
 * const app = express()
 *
 * // Create OAuth router
 * const router = payments.mcp.createRouter({
 *   baseUrl: 'http://localhost:5001',
 *   agentId: 'agent_123',
 *   serverName: 'my-mcp-server'
 * })
 *
 * app.use(router)
 * app.listen(5001)
 * ```
 */
import type { Payments } from '../payments.js';
import type { EnvironmentName } from '../environments.js';
import type { McpToolConfig, McpResourceConfig, McpPromptConfig, McpRegistrationOptions, ToolHandler, ResourceHandler, PromptHandler, McpServerConfig, McpServerResult } from './types/server.types.js';
import type { ToolOptions, ResourceOptions, PromptOptions, PaywallContext, McpConfig } from './types/paywall.types.js';
import type { HttpServerResult } from './types/http.types.js';
export type { CreditsContext, CreditsOption, PaywallOptions, McpConfig, PaywallContext, AuthResult, } from './types/paywall.types.js';
export type { OAuthUrls, OAuthConfig, HttpRouterConfig, HttpServerConfig, HttpServerResult, ProtectedResourceMetadata, McpProtectedResourceMetadata, AuthorizationServerMetadata, OidcConfiguration, ClientRegistrationRequest, ClientRegistrationResponse, ServerInfoResponse, } from './types/http.types.js';
export { buildExtraFromHttpHeaders, buildExtraFromHttpRequest } from './utils/extra.js';
export type { McpToolConfig, McpResourceConfig, McpPromptConfig, McpRegistrationOptions, ToolHandler, ResourceHandler, PromptHandler, McpServerConfig, McpServerResult, ServerInfo, ToolContext, ResourceContext, PromptContext, } from './types/server.types.js';
export { getOAuthUrls, buildProtectedResourceMetadata, buildMcpProtectedResourceMetadata, buildAuthorizationServerMetadata, buildOidcConfiguration, buildServerInfoResponse, createOAuthRouter, createCorsMiddleware, createJsonMiddleware, createRequireAuthMiddleware, createHttpLoggingMiddleware, startManagedServer, createMcpApp, ClientRegistrationError, } from './http/index.js';
/**
 * Extended MCP configuration including HTTP server options.
 */
export interface ExtendedMcpConfig extends McpConfig {
    /** Base URL for the server (required for HTTP features) */
    baseUrl?: string;
    /** Nevermined environment */
    environment?: EnvironmentName;
    /** Tools exposed by this server */
    tools?: string[];
    /** Custom OAuth scopes */
    scopes?: string[];
}
/**
 * Options for creating the OAuth router.
 */
export interface CreateRouterOptions {
    /** Base URL of the MCP server */
    baseUrl: string;
    /** Agent ID (client_id) */
    agentId?: string;
    /** Server name */
    serverName?: string;
    /** Tools exposed by this server */
    tools?: string[];
    /** Enable OAuth discovery endpoints */
    enableOAuthDiscovery?: boolean;
    /** Enable client registration */
    enableClientRegistration?: boolean;
    /** Enable health check */
    enableHealthCheck?: boolean;
    /** Enable server info */
    enableServerInfo?: boolean;
    /** Server version */
    version?: string;
    /** Server description */
    description?: string;
}
/**
 * Options for starting the managed server.
 */
export interface StartServerOptions {
    /** Port to listen on */
    port: number;
    /** Host to bind to */
    host?: string;
    /** Base URL (defaults to http://localhost:\{port\}) */
    baseUrl?: string;
    /** Agent ID (uses configured agentId if not provided) */
    agentId?: string;
    /** Server name */
    serverName?: string;
    /** Tools exposed by this server */
    tools?: string[];
    /** Enable OAuth discovery endpoints */
    enableOAuthDiscovery?: boolean;
    /** Enable client registration */
    enableClientRegistration?: boolean;
    /** Enable health check */
    enableHealthCheck?: boolean;
    /** Enable server info */
    enableServerInfo?: boolean;
    /** Server version */
    version?: string;
    /** Server description */
    description?: string;
    /** Callback when server starts */
    onStart?: (result: HttpServerResult) => void;
    /** Logging callback */
    onLog?: (message: string, level?: 'info' | 'warn' | 'error') => void;
}
/**
 * Build MCP integration with modular architecture.
 * This function creates the complete MCP API surface including
 * paywall protection, OAuth endpoints, and HTTP server management.
 *
 * @param paymentsService - The Payments instance
 * @returns MCP integration API
 */
export declare function buildMcpIntegration(paymentsService: Payments): {
    configure: (options: ExtendedMcpConfig) => void;
    withPaywall: {
        <TArgs = any>(handler: (args: TArgs, extra?: any, context?: PaywallContext) => Promise<any> | any, options: ToolOptions | PromptOptions): (args: TArgs, extra?: any) => Promise<any>;
        <TArgs = any>(handler: (args: TArgs, extra?: any, context?: PaywallContext) => Promise<any> | any): (args: TArgs, extra?: any) => Promise<any>;
        (handler: (uri: URL, variables: Record<string, string | string[]>, extra?: any, context?: PaywallContext) => Promise<any> | any, options: ResourceOptions): (uri: URL, variables: Record<string, string | string[]>, extra?: any) => Promise<any>;
    };
    attach: (server: {
        registerTool: (name: string, config: any, handler: any) => void;
        registerResource: (name: string, template: any, config: any, handler: any) => void;
        registerPrompt: (name: string, config: any, handler: any) => void;
    }) => {
        registerTool<TArgs = any>(name: string, config: any, handler: (args: TArgs, extra?: any, context?: PaywallContext) => Promise<any> | any, options?: Omit<ToolOptions, "kind" | "name">): void;
        registerResource(name: string, template: any, config: any, handler: (uri: URL, variables: Record<string, string | string[]>, extra?: any, context?: PaywallContext) => Promise<any> | any, options?: Omit<ResourceOptions, "kind" | "name">): void;
        registerPrompt<TArgs = any>(name: string, config: any, handler: (args: TArgs, extra?: any, context?: PaywallContext) => Promise<any> | any, options?: Omit<PromptOptions, "kind" | "name">): void;
    };
    authenticateMeta: (extra: any, options: {
        planId?: string;
    } | undefined, method: string) => Promise<import("./types/paywall.types.js").AuthResult>;
    createRouter: (options: CreateRouterOptions) => import("express").Router;
    createApp: (options: Omit<CreateRouterOptions, "agentId"> & {
        agentId?: string;
    }) => import("express").Application;
    startServer: (options: StartServerOptions) => Promise<HttpServerResult>;
    getConfig: () => ExtendedMcpConfig;
    registerTool: <Args = any>(name: string, config: McpToolConfig, handler: ToolHandler<Args>, options?: McpRegistrationOptions) => void;
    registerResource: (name: string, uriOrTemplate: string, config: McpResourceConfig, handler: ResourceHandler, options?: McpRegistrationOptions) => void;
    registerPrompt: <Args = any>(name: string, config: McpPromptConfig, handler: PromptHandler<Args>, options?: McpRegistrationOptions) => void;
    start: (config: McpServerConfig) => Promise<McpServerResult>;
    stop: () => Promise<void>;
};
//# sourceMappingURL=index.d.ts.map