import type { Payments } from '../../payments.js';
import { McpConfig, PromptOptions, ResourceOptions, ToolOptions } from '../types/paywall.types.js';
import { PaywallAuthenticator } from './auth.js';
import { CreditsContextProvider } from './credits-context.js';
/**
 * Main class for creating paywall-protected MCP handlers
 */
export declare class PaywallDecorator {
    private payments;
    private authenticator;
    private creditsContext;
    private config;
    constructor(payments: Payments, authenticator: PaywallAuthenticator, creditsContext: CreditsContextProvider);
    /**
     * Configure the paywall with agent and server information
     */
    configure(options: McpConfig): void;
    /**
     * Create a paywall-protected handler (uncurried version only)
     */
    protect<TArgs = any>(handler: (args: TArgs, extra?: any) => Promise<any> | any, options: ToolOptions | PromptOptions): (args: TArgs, extra?: any) => Promise<any>;
    protect(handler: (uri: URL, variables: Record<string, string | string[]>, extra?: any) => Promise<any> | any, options: ResourceOptions): (uri: URL, variables: Record<string, string | string[]>, extra?: any) => Promise<any>;
    /**
     * Internal method to create the wrapped handler
     */
    private createWrappedHandler;
    /**
     * Redeem credits after successful request
     */
    private redeemCredits;
}
//# sourceMappingURL=paywall.d.ts.map